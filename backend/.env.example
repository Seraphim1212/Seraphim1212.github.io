# Copie este arquivo para backend/.env e ajuste os valores.

PORT=3000

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=geral

# Origens permitidas pelo CORS, separadas por vírgula.
# Use "*" para liberar qualquer origem (somente em desenvolvimento local).
CORS_ORIGIN=*
